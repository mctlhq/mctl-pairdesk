{
  "name": "mctl-pairdesk",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "PairDesk: a closed Telegram Mini App + bot where vetted community members post and match P2P exchange requests (EUR/RUB/USDT). A bulletin board, not an exchange: no custody, no payments, no escrow.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build:landing && npm run build:web && npm run build:api",
    "build:landing": "cd landing && npm ci && npm run build",
    "build:web": "cd web && npm install && npm run build",
    "build:api": "tsc -p tsconfig.json && cp src/db/schema.sql dist/db/schema.sql",
    "start": "node dist/server.js",
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2"
  }
}
